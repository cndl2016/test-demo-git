server:
  port: 7777

spring:
  application:
    name: openai

  ai:
    openai:
      api-key: sk-54034f01de884cfba1a05679d441e51f
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen-plus
        completions-path: /v1/chat/completions
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: always
            schema-locations: classpath:schema-mysql.sql

  datasource:
    url: jdbc:mysql://localhost:3306/test_openai?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&allowMultiQueries=true&tinyInt1isBit=false&allowLoadLocalInfile=true&allowLocalInfile=true&allowUrl
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver