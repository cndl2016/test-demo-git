server:
  port: 7001

spring:
  main:
    banner-mode: off
  ai:
    mcp:
      server:
        name: my-mcp-server
        version: 1.0.0
        type: SYNC
        sse-endpoint: /sse/mcp
        capabilities:
          tool: true
          resource: true
          prompt: true
          completion: true